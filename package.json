{
  "name": "delayed-rabbit",
  "version": "1.0.0",
  "description": "Testing combination of  TTL and DLX to delay message delivery on RabbitMQ",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "rimraf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "alisonsm92",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.8.0",
    "dotenv": "^16.0.0",
    "pino": "^7.10.0",
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "@types/amqplib": "^0.8.2",
    "pino-pretty": "^7.6.1",
    "rimraf": "^3.0.2"
  }
}
